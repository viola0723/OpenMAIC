# 锚点.md — openmaic（OpenMAIC 二开项目）

> 给新会话的 Kimi Code：读这份 30 秒上手。每次阶段收尾重写「当前状态」。

## 项目一句话

清华 THU-MAIC/OpenMAIC（多智能体互动课堂，MIT 协议）的本地克隆 + 二次开发项目（2026-09-04 V 拍板立项）。
目标共识：**A 路线 = 本地厨房做课 → 导出自包含 HTML/MP4 → 进 win98 展出**（随身听同款玩法）；
**B 路线 = 实时互动课堂部署 tx-cloud**（实时讨论/自由提问是项目精华，必须有活后端；照 VoiceBridge 模板：
ubuntu 用户级 Node + PM2 + nginx 反代 + `ACCESS_CODE` 口令闸门，不动 OpenClaw）。先 A 后 B。

## 上游与 remote

- 上游官方仓：https://github.com/THU-MAIC/OpenMAIC （2026-09-04 克隆时 31k star，v1.0.0 刚发布）
- **fork 已完成【V 操作 2026-09-04】**：`origin` = viola0723/OpenMAIC（push 目标），`upstream` = THU-MAIC/OpenMAIC
  （跟进官方更新：`git fetch upstream && git merge upstream/main` 或 rebase，完事 push 到 origin）
- core.sshCommand 已写入仓库 config，裸 `git pull/push/fetch` 即可，不用带密钥参数
- 在线体验/官方实例：https://open.maic.chat/

## 当前状态

- 2026-09-04｜**立项日 ✅ 全链路打通 + 首课诞生**：克隆 → 装依赖 → 冒烟 → fork/remote → `.env.local`（MiniMax M3 + TTS）→
  **首课「傅里叶变换直觉入门」生成成功**（job `6X6C6fD-5C`，classroom `f-NvZjK_T8`，7 场景约 3.5 分钟，
  LLM ~9.8 万 token/课 实测）。场景类型全验：slides（图文卡片）、互动仿真（波形叠加器，5 谐波通道滑杆）、
  白板推导（KaTeX 公式）、图解对比页；TTS 旁白 35 段音频。
- **导出实测结论（A 路线关键验证）**：①课堂 ZIP = manifest.json + 音频，仅迁移/存档用，不可独立播；
  ②**教学资源包 = 可编辑 PPTX + 自包含互动 HTML，纯静态 http 服务下脱离 OpenMAIC 完整可玩** → A 路线产物成立；
  ③实时讨论/自由提问必须活后端（B 路线的理由坐实）。
- dev server 已按纪律收掉（测完即关，3000 端口已验空）。日常使用：项目目录跑 `pnpm dev` 即可。
- **V 进班实测结论（2026-09-04）：超预期**。讨论自然、自由提问强、各角色音色各异；V 点出关键洞察——
  这项目的核心玩法是**模型配置与资源分配**（UI 设置面非常完善：AI 同学阵容、音色、思考等级、各模态开关都在界面里）。

## 环境备忘

- 依赖安装：`npm_config_registry=https://mirrors.cloud.tencent.com/npm/ pnpm install`（本机 npm 默认源极慢，勿省前缀）
- 跑：`pnpm dev` → http://localhost:3000（Node 24.18 / pnpm 10.34 实测可跑；仓库 packageManager 会拉 pnpm 10.28 执行，正常）
- **杀 dev server 后必须验证端口**：`netstat -ano | grep ":3000"` 无 LISTENING 才算完；pnpm 壳杀不净 Node 子进程，
  残留按 PID `taskkill //PID <pid> //F`（2026-09-04 实测复现）
- pnpm 报 "Ignored build scripts"（esbuild/sharp/onnxruntime 等）暂不影响；遇到原生模块异常再 `pnpm approve-builds`
- git 提交身份为仓库级配置（viola0723 + GitHub noreply 邮箱，与 win98 同款，2026-09-04 配）；本机无全局 git 身份，新克隆仓库记得照样配一遍

## 配置清单（密钥 V 经手、永不进对话/git）

**选型 = MiniMax 全家桶（Token Plan）【V 定 2026-09-04】；落地方式 = 项目根 `.env.local`（V 选择，2026-09-04 已配好并验证通过）**。

已验证状态：`/api/server-providers` 显示 minimax（LLM，钉 MiniMax-M3）+ minimax-tts 点亮；
`/api/verify-model` 真实调用 `minimax:MiniMax-M3` 返回 OK（2026-09-04）。
另：本机 OS 环境变量里存量的 DEEPSEEK/GLM key 也被自动点亮，UI 里会多出模型可选项，无副作用。

**BASE_URL 不用填**：代码默认值即正确值——LLM `https://api.minimaxi.com/anthropic/v1`（Anthropic 兼容端点，
`providers.ts:1047`），TTS/图像/视频 `https://api.minimaxi.com`（裸域，代码自拼路径）。内置 Token Plan 预设见
`lib/config/token-plan-presets.ts:79`（MiniMax 全模态：LLM/图/视频/TTS/搜索）。

- ASR 起步不填：文字提问即可，另有浏览器原生语音识别兜底（无需 key）
- 可选：B 部署时 `ACCESS_CODE` 必开；以后想配图再说 `IMAGE_MINIMAX_API_KEY`
- TTS 模型/音色不在 env，在**设置 UI** 里选：MiniMax 内置 6 模型（默认 `speech-2.8-hd` 质量优先，turbo 速度优先），默认音色 `female-yujie`
- 思考（thinking）配置三层：`LLM_THINKING_DISABLED` 全局 / `MODEL_ROUTES` 按阶段 / 设置 UI 按模型；
  **MiniMax M3/M2.7 未声明 thinking 能力，无控件、按服务端默认行为**；DeepSeek 支持开/关
- 踩坑【已录】：`.env.local` 文件名错一个字符都不加载（V 误命名 `.env.local.example`，改名 + 重启 dev server 后才生效；
  Next.js 改 env 文件必须重启 dev server）。验证配置是否加载：`curl localhost:3000/api/server-providers`
- 踩坑【已录】：**`DEFAULT_MODEL` 必填**（格式 `厂商:模型名`，如 `minimax:MiniMax-M3`）：API 生成路径没有浏览器 x-model 兜底，
  空值直接报 "No model could be resolved"（2026-09-04 任务 J3w7OJBuMX 实测）；且值要落在 `MINIMAX_MODELS` 钉的目录内

## 候选二开点（挂账，不急）

- **接腾讯云 ASR/TTS**（VoiceBridge 已开通、有免费额度）：OpenMAIC 支持自定义 TTS/ASR 提供商（v0.1.1+），
  写适配层即可。触发时机：MiniMax 额度吃紧 / B 部署想统一语音栈 / 想白嫖腾讯云免费额度
- **定制角色音色**（如修仙课师父/师妹）：音色机制 = LLM 只出人设（名/角色/性格，服务端阵容 voiceless by design），
  音色解析 = 手动指定 > 角色绑定 > 确定性轮转（`voice-resolver.ts:158`，序号 % 目录数，老师用全局音色）。
  现成路径：设置 UI 手动绑；进阶路径：`voiceDesign` 三层配方（identity/texture/delivery → SHA-256 确定性 id）
  仅 VoxCPM 类"声音提示词"提供商生效，MiniMax 目录制用不上

## 待 V 项

1. 定下一步方向（2026-09-04 测试收官后拍板）：A 展品化（课程产物怎么进 win98）/ B 部署排期（tx-cloud）/ 继续出课攒样本
